import React from "react";
import { MapContainer } from "react-leaflet";

const MapBox = () => {
  return (
    <div className="map">
      <h2>Map section</h2>
    </div>
  );
};

export default MapBox;
